<div class="form-actions">
	<%= link_to 'Adicionar Professor', new_professor_path, :class => "btn btn-primary" %>
</div>
<h1>Lista de Professores</h1>

<table class="table table-bordered">
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @professors.each do |professor| %>
  <tr>
    <td><%= professor.nome %></td>
    <td><%= link_to 'Ver+', professor, :class => 'btn btn-info' %></td>
    <td><%= link_to 'Editar', edit_professor_path(professor), :class => 'btn btn-warning' %></td>
    <td><%= link_to 'Apagar', professor, confirm: 'Você tem certeza?', method: :delete, :class => 'btn btn-danger' %></td>
  </tr>
<% end %>
</table>

<br />


